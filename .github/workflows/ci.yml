# Placeholder CI workflow for DF StoryForge.
# Uncomment and adjust once the monorepo has package.json scripts and tests.
#
# name: CI
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
# jobs:
#   lint-and-test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: pnpm/action-setup@v2
#         with:
#           version: 8
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'pnpm'
#       - run: pnpm install --frozen-lockfile
#       - run: pnpm run lint
#       - run: pnpm run test
